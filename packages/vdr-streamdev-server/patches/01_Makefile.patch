--- vdr-streamdev-server-0.6.0/Makefile.org	2012-12-27 22:43:39.836576346 +0100
+++ vdr-streamdev-server-0.6.0/Makefile	2012-12-27 22:43:50.344576527 +0100
@@ -34,7 +34,7 @@
 ### Allow user defined options to overwrite defaults:
 
 ifeq ($(shell test $(APIVERSNUM) -ge 10713; echo $$?),0)
-include $(VDRDIR)/Make.global
+-include $(VDRDIR)/Make.global
 else
 ifeq ($(shell test $(APIVERSNUM) -ge 10704 -o -n "$(TSPLAYVERSNUM)" ; echo $$?),0)
 DEFINES  += -D_FILE_OFFSET_BITS=64 -D_LARGEFILE_SOURCE -D_LARGEFILE64_SOURCE
